<p id="notice"><%= notice %></p>
<div class="container">
<div class="btn-group" style="color:#0000FF">
<button class="btn btn-success btn-block">
<h1>Genres</h1>
</button>
</div>
<br>
<table class="table table-responsive">
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @genres.each do |genre| %>
      <tr>
        <td><%= genre.title %></td>
        <td><%= link_to 'view','/genres/'+(genre.id).to_s+'/subgenres/' %></td>
        <!--<td><%= link_to 'Show', genre %></td>
        <td><%= link_to 'Edit', edit_genre_path(genre) %></td>
        <td><%= link_to 'Destroy', genre, method: :delete, data: { confirm: 'Are you sure?' } %></td>-->
      </tr>
    <% end %>
  </tbody>
</table>

<br>
</div>
<!--<%= link_to 'New Genre', new_genre_path %>-->
